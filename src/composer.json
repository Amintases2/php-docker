{
    "autoload": {
        "psr-4": {
            "App\\": "app"
        }
    },
    "require-dev": {
        "symfony/var-dumper": "^7.0",
        "phpunit/phpunit": "^11.1"
    },
    "require": {
        "pfw/framework": "dev-main",
        "symfony/dotenv": "^7.1"
    },
    "repositories": [
        {
            "type": "path",
            "url": "framework"
        }
    ],

    "config": {
        "allow-plugins": {
            "php-http/discovery": true
        }
    }
}
