{
    "autoload": {
        "psr-4": {
            "App\\": "app"
        }
    },
    "scripts": {
        "test": "phpunit tests"
    },
    "require-dev": {
        "symfony/var-dumper": "^7.0",
        "phpunit/phpunit": "^11.1"
    },
    "require": {
        "pfw/framework": "dev-main"
    },
    "repositories": [
        {
            "type": "path",
            "url": "framework"
        }
    ],

    "config": {
        "allow-plugins": {
            "php-http/discovery": true
        }
    }
}
